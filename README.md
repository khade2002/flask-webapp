# Flask Web App for CI/CD Demo

This is a simple Flask-based web application used to demonstrate Continuous Integration and Continuous Deployment (CI/CD) using Jenkins and Ansible.

## How to Run
```bash
pip install -r requirements.txt
python app.py
